<form method="post" action="{{ path('app_admin_job_delete', {'id': job.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet offre d'emploie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
