{% extends 'base.html.twig' %}

{% block title %}Edit Job{% endblock %}

{% block body %}
<div class="container w-75">
    <h1>Modifier l'annoncen°{{ job.id }}</h1>
    <div class="container text-center">
        {{ include('admin_job/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <a class="text-danger text-decoration-none" href="{{ path('app_admin_job_index') }}">Retour à la liste</a>
        {{ include('admin_job/_delete_form.html.twig') }}
    </div>
</div>
    
{% endblock %}
